spring:
  application:
    name: ddsh-base-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/asan3524/config
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest